package com.radyfy.common.model.enums.grid;

public enum ActionGridType {
      // ACTION types
      popupForm, popupTable, confirm, download,private_download,expendedTable, expendedForm, table_download, slug, form_submit;
}
