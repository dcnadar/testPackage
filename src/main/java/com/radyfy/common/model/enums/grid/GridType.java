package com.radyfy.common.model.enums.grid;

public enum GridType {
    // grid types for crm
    table, form, menu, account_filter, dashboard,
    calendar, calendar_table, chart,
    progress_cards, iframe;
}
